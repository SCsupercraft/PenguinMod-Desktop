{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "changelog",
  "description": "Capabilities used by the changelog window",
  "windows": ["changelog"],
  "permissions": ["core:window:allow-close"]
}
